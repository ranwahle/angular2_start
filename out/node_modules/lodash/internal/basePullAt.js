System.registerModule("../../../../node_modules/lodash/internal/basePullAt.js", [], function() {
  "use strict";
  var __moduleName = "../../../../node_modules/lodash/internal/basePullAt.js";
  var baseAt = require('./baseAt'),
      baseCompareAscending = require('./baseCompareAscending'),
      isIndex = require('./isIndex');
  var arrayProto = Array.prototype;
  var splice = arrayProto.splice;
  function basePullAt(array, indexes) {
    var length = indexes.length,
        result = baseAt(array, indexes);
    indexes.sort(baseCompareAscending);
    while (length--) {
      var index = parseFloat(indexes[length]);
      if (index != previous && isIndex(index)) {
        var previous = index;
        splice.call(array, index, 1);
      }
    }
    return result;
  }
  module.exports = basePullAt;
  return {};
});
System.get("../../../../node_modules/lodash/internal/basePullAt.js" + '');
