System.registerModule("../../../../node_modules/lodash/internal/lazyClone.js", [], function() {
  "use strict";
  var __moduleName = "../../../../node_modules/lodash/internal/lazyClone.js";
  var LazyWrapper = require('./LazyWrapper'),
      arrayCopy = require('./arrayCopy');
  function lazyClone() {
    var actions = this.actions,
        iteratees = this.iteratees,
        views = this.views,
        result = new LazyWrapper(this.wrapped);
    result.actions = actions ? arrayCopy(actions) : null;
    result.dir = this.dir;
    result.dropCount = this.dropCount;
    result.filtered = this.filtered;
    result.iteratees = iteratees ? arrayCopy(iteratees) : null;
    result.takeCount = this.takeCount;
    result.views = views ? arrayCopy(views) : null;
    return result;
  }
  module.exports = lazyClone;
  return {};
});
System.get("../../../../node_modules/lodash/internal/lazyClone.js" + '');
