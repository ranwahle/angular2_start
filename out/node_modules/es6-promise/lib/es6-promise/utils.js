System.registerModule("../../../../../node_modules/es6-promise/lib/es6-promise/utils.js", [], function() {
  "use strict";
  var __moduleName = "../../../../../node_modules/es6-promise/lib/es6-promise/utils.js";
  function objectOrFunction(x) {
    return typeof x === 'function' || (typeof x === 'object' && x !== null);
  }
  function isFunction(x) {
    return typeof x === 'function';
  }
  function isMaybeThenable(x) {
    return typeof x === 'object' && x !== null;
  }
  var _isArray;
  if (!Array.isArray) {
    _isArray = function(x) {
      return Object.prototype.toString.call(x) === '[object Array]';
    };
  } else {
    _isArray = Array.isArray;
  }
  var isArray = _isArray;
  return {
    get objectOrFunction() {
      return objectOrFunction;
    },
    get isFunction() {
      return isFunction;
    },
    get isMaybeThenable() {
      return isMaybeThenable;
    },
    get isArray() {
      return isArray;
    }
  };
});
System.get("../../../../../node_modules/es6-promise/lib/es6-promise/utils.js" + '');
