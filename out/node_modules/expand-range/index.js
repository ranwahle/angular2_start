System.registerModule("../../../node_modules/expand-range/index.js", [], function() {
  "use strict";
  var __moduleName = "../../../node_modules/expand-range/index.js";
  var fill = require('fill-range');
  module.exports = function expandRange(str, options, fn) {
    if (typeof str !== 'string') {
      throw new TypeError('expand-range expects a string.');
    }
    if (typeof options === 'function') {
      fn = options;
      options = {};
    }
    if (typeof options === 'boolean') {
      options = {};
      options.makeRe = true;
    }
    var opts = options || {};
    var args = str.split('..');
    var len = args.length;
    if (len > 3) {
      return str;
    }
    if (len === 1) {
      return args;
    }
    if (typeof fn === 'boolean' && fn === true) {
      opts.makeRe = true;
    }
    args.push(opts);
    return fill.apply(fill, args.concat(fn));
  };
  return {};
});
System.get("../../../node_modules/expand-range/index.js" + '');
