System.registerModule("../../../node_modules/is-my-json-valid/require.js", [], function() {
  "use strict";
  var __moduleName = "../../../node_modules/is-my-json-valid/require.js";
  var fs = require('fs');
  var path = require('path');
  var compile = require('./');
  delete require.cache[require.resolve(__filename)];
  module.exports = function(file, opts) {
    file = path.join(path.dirname(module.parent.filename), file);
    if (!fs.existsSync(file) && fs.existsSync(file + '.schema'))
      file += '.schema';
    if (!fs.existsSync(file) && fs.existsSync(file + '.json'))
      file += '.json';
    return compile(fs.readFileSync(file, 'utf-8'), opts);
  };
  return {};
});
System.get("../../../node_modules/is-my-json-valid/require.js" + '');
