System.registerModule("../../../../node_modules/sntp/examples/time.js", [], function() {
  "use strict";
  var __moduleName = "../../../../node_modules/sntp/examples/time.js";
  var Sntp = require('../lib');
  var options = {
    host: 'nist1-sj.ustiming.org',
    port: 123,
    resolveReference: true,
    timeout: 1000
  };
  Sntp.time(options, function(err, time) {
    if (err) {
      console.log('Failed: ' + err.message);
      process.exit(1);
    }
    console.log(time);
    console.log('Local clock is off by: ' + time.t + ' milliseconds');
    process.exit(0);
  });
  return {};
});
System.get("../../../../node_modules/sntp/examples/time.js" + '');
