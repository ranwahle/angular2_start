System.registerModule("../../../../../../../../node_modules/angular2/es6/prod/src/core/linker/event_config.js", [], function() {
  "use strict";
  var __moduleName = "../../../../../../../../node_modules/angular2/es6/prod/src/core/linker/event_config.js";
  var EVENT_TARGET_SEPARATOR = ':';
  var EventConfig = function() {
    function EventConfig(fieldName, eventName, isLongForm) {
      this.fieldName = fieldName;
      this.eventName = eventName;
      this.isLongForm = isLongForm;
    }
    return ($traceurRuntime.createClass)(EventConfig, {getFullName: function() {
        return this.isLongForm ? ("" + this.fieldName + EVENT_TARGET_SEPARATOR + this.eventName) : this.eventName;
      }}, {parse: function(eventConfig) {
        var fieldName = eventConfig,
            eventName = eventConfig,
            isLongForm = false;
        var separatorIdx = eventConfig.indexOf(EVENT_TARGET_SEPARATOR);
        if (separatorIdx > -1) {
          fieldName = eventConfig.substring(0, separatorIdx).trim();
          eventName = eventConfig.substring(separatorIdx + 1).trim();
          isLongForm = true;
        }
        return new EventConfig(fieldName, eventName, isLongForm);
      }});
  }();
  return {
    get EVENT_TARGET_SEPARATOR() {
      return EVENT_TARGET_SEPARATOR;
    },
    get EventConfig() {
      return EventConfig;
    }
  };
});
System.get("../../../../../../../../node_modules/angular2/es6/prod/src/core/linker/event_config.js" + '');
