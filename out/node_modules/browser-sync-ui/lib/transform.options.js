System.registerModule("../../../../node_modules/browser-sync-ui/lib/transform.options.js", [], function() {
  "use strict";
  var __moduleName = "../../../../node_modules/browser-sync-ui/lib/transform.options.js";
  var path = require("path");
  module.exports = function(bs) {
    var options = bs.options;
    var server = options.server;
    var cwd = bs.cwd;
    if (server) {
      if (Array.isArray(server.baseDir)) {
        server.baseDirs = options.server.baseDir.map(function(item) {
          return path.join(cwd, item);
        });
      } else {
        server.baseDirs = [path.join(cwd, server.baseDir)];
      }
    }
  };
  return {};
});
System.get("../../../../node_modules/browser-sync-ui/lib/transform.options.js" + '');
