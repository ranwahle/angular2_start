System.registerModule("../../../../../node_modules/fsevents/node_modules/lodash._createpadding/index.js", [], function() {
  "use strict";
  var __moduleName = "../../../../../node_modules/fsevents/node_modules/lodash._createpadding/index.js";
  var repeat = require('lodash.repeat');
  var nativeCeil = Math.ceil,
      nativeIsFinite = global.isFinite;
  function createPadding(string, length, chars) {
    var strLength = string.length;
    length = +length;
    if (strLength >= length || !nativeIsFinite(length)) {
      return '';
    }
    var padLength = length - strLength;
    chars = chars == null ? ' ' : (chars + '');
    return repeat(chars, nativeCeil(padLength / chars.length)).slice(0, padLength);
  }
  module.exports = createPadding;
  return {};
});
System.get("../../../../../node_modules/fsevents/node_modules/lodash._createpadding/index.js" + '');
