System.registerModule("../../../../../node_modules/fsevents/node_modules/lodash.pad/index.js", [], function() {
  "use strict";
  var __moduleName = "../../../../../node_modules/fsevents/node_modules/lodash.pad/index.js";
  var baseToString = require('lodash._basetostring'),
      createPadding = require('lodash._createpadding');
  var nativeCeil = Math.ceil,
      nativeFloor = Math.floor,
      nativeIsFinite = global.isFinite;
  function pad(string, length, chars) {
    string = baseToString(string);
    length = +length;
    var strLength = string.length;
    if (strLength >= length || !nativeIsFinite(length)) {
      return string;
    }
    var mid = (length - strLength) / 2,
        leftLength = nativeFloor(mid),
        rightLength = nativeCeil(mid);
    chars = createPadding('', rightLength, chars);
    return chars.slice(0, leftLength) + string + chars;
  }
  module.exports = pad;
  return {};
});
System.get("../../../../../node_modules/fsevents/node_modules/lodash.pad/index.js" + '');
