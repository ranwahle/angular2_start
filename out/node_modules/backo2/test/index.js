System.registerModule("../../../../node_modules/backo2/test/index.js", [], function() {
  "use strict";
  var __moduleName = "../../../../node_modules/backo2/test/index.js";
  var Backoff = require('..');
  var assert = require('assert');
  describe('.duration()', function() {
    it('should increase the backoff', function() {
      var b = new Backoff;
      assert(100 == b.duration());
      assert(200 == b.duration());
      assert(400 == b.duration());
      assert(800 == b.duration());
      b.reset();
      assert(100 == b.duration());
      assert(200 == b.duration());
    });
  });
  return {};
});
System.get("../../../../node_modules/backo2/test/index.js" + '');
