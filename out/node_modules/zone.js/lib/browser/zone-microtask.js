System.registerModule("../../../../../node_modules/zone.js/lib/browser/zone-microtask.js", [], function() {
  "use strict";
  var __moduleName = "../../../../../node_modules/zone.js/lib/browser/zone-microtask.js";
  var core = require('../core');
  var microtask = require('../microtask');
  var browserPatch = require('../patch/browser');
  var es6Promise = require('es6-promise');
  if (global.Zone) {
    console.warn('Zone already exported on window the object!');
  }
  global.Zone = microtask.addMicrotaskSupport(core.Zone);
  global.zone = new global.Zone();
  global.Promise = es6Promise.Promise;
  browserPatch.apply();
  return {};
});
System.get("../../../../../node_modules/zone.js/lib/browser/zone-microtask.js" + '');
