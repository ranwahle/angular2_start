System.registerModule("../../../node_modules/repeat-string/index.js", [], function() {
  "use strict";
  var __moduleName = "../../../node_modules/repeat-string/index.js";
  module.exports = repeat;
  function repeat(str, num) {
    if (typeof str !== 'string') {
      throw new TypeError('repeat-string expects a string.');
    }
    if (num === 1)
      return str;
    if (num === 2)
      return str + str;
    var max = str.length * num;
    if (cache !== str || typeof cache === 'undefined') {
      cache = str;
      res = '';
    }
    while (max > res.length && num > 0) {
      if (num & 1) {
        res += str;
      }
      num >>= 1;
      if (!num)
        break;
      str += str;
    }
    return res.substr(0, max);
  }
  var res = '';
  var cache;
  return {};
});
System.get("../../../node_modules/repeat-string/index.js" + '');
